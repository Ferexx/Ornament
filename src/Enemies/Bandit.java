package Enemies;

import Main.Game;
import Main.ID;

public class Bandit extends HumanEnemy {
    public Bandit(int x, int y, ID id, Game game, int health) {
        super(x, y, id, game, health);
    }
}
