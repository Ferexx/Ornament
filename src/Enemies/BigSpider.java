package Enemies;

import Main.Game;
import Main.ID;

public class BigSpider extends Spider {
    public BigSpider(int x, int y, ID id, Game game, int health) {
        super(x, y, id, game, health);
    }
}
