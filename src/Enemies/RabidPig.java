package Enemies;

import Main.Game;
import Main.ID;

public class RabidPig extends NatureEnemy {
    public RabidPig(int x, int y, ID id, Game game, int health) {
        super(x, y, id, game, health);
    }
}
