package Enemies;

import Main.Game;
import Main.ID;

public class WeakSpider extends Spider {
    public WeakSpider(int x, int y, ID id, Game game, int health) {
        super(x, y, id, game, health);
    }
}
